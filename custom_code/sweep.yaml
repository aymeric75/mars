method: grid
metric:
  name: val_loss
  goal: minimize

parameters:
  train_fraction:
    values: [1.0, 0.5]

  model_size:
    values: ["base", "small"]

  # optional: also sweep seed if you want
  seed:
    values: [123]
